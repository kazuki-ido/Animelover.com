<h1>All users</h1>

<ul class= "users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag user.image.url, class: "icon_image" %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>